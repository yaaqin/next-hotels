version: "3.9"
services:
  next-app:
    container_name: next-mbsc
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:9022:9022"
    env_file:
      - .env
    restart: always